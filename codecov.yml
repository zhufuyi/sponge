comment: false

github_checks:
  annotations: false

coverage:
  status:
    project:
      default:
        target: '75'
    patch:
      default:
        target: '60'
        informational: true

ignore:
  - "vendor/**/*"
  - "**.pb.go"
